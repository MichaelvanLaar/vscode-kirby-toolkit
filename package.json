{
  "name": "vscode-kirby-toolkit",
  "displayName": "Kirby CMS Developer Toolkit",
  "description": "Productivity tools for Kirby CMS development: type-hints, Blueprint validation, and snippet navigation",
  "version": "0.4.0",
  "license": "MIT",
  "author": {
    "name": "Michael van Laar",
    "email": "michael@van-laar.de"
  },
  "homepage": "https://github.com/MichaelvanLaar/vscode-kirby-toolkit",
  "publisher": "MichaelvanLaar",
  "repository": {
    "type": "git",
    "url": "https://github.com/MichaelvanLaar/vscode-kirby-toolkit"
  },
  "bugs": {
    "url": "https://github.com/MichaelvanLaar/vscode-kirby-toolkit/issues"
  },
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets",
    "Other"
  ],
  "keywords": [
    "kirby",
    "kirby cms",
    "php",
    "yaml",
    "blueprints"
  ],
  "activationEvents": [
    "onLanguage:php",
    "onLanguage:yaml",
    "workspaceContains:site/"
  ],
  "main": "./out/extension.js",
  "extensionDependencies": [
    "redhat.vscode-yaml"
  ],
  "contributes": {
    "commands": [
      {
        "command": "kirby.addTypeHints",
        "title": "Kirby: Add Type Hints"
      },
      {
        "command": "kirby.newPageType",
        "title": "Kirby: New Page Type"
      },
      {
        "command": "kirby.extractToSnippet",
        "title": "Kirby: Extract to Snippet"
      },
      {
        "command": "kirby.configureTailwind",
        "title": "Kirby: Configure Tailwind IntelliSense"
      },
      {
        "command": "kirby.resetTailwindPrompt",
        "title": "Kirby: Reset Tailwind Integration Prompt"
      },
      {
        "command": "kirby.resetSyncPrompts",
        "title": "Kirby: Reset Blueprint/Template Sync Prompts"
      },
      {
        "command": "kirby.startBuildWatcher",
        "title": "Kirby: Start Build Watcher"
      },
      {
        "command": "kirby.stopBuildWatcher",
        "title": "Kirby: Stop Build Watcher"
      },
      {
        "command": "kirby.restartBuildWatcher",
        "title": "Kirby: Restart Build Watcher"
      },
      {
        "command": "kirby.runBuildOnce",
        "title": "Kirby: Run Build Once"
      },
      {
        "command": "kirby.showBuildTerminal",
        "title": "Kirby: Show Build Terminal"
      },
      {
        "command": "kirby.removeApiStubs",
        "title": "Kirby: Remove API Stubs"
      },
      {
        "command": "kirby.reinstallApiStubs",
        "title": "Kirby: Reinstall API Stubs"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "kirby.extractToSnippet",
          "when": "editorHasSelection && resourceLangId == php",
          "group": "1_modification"
        }
      ]
    },
    "configuration": {
      "title": "Kirby CMS Developer Toolkit",
      "properties": {
        "kirby.autoInjectTypeHints": {
          "type": "boolean",
          "default": true,
          "description": "Automatically inject PHPDoc type hints when creating new template or snippet files"
        },
        "kirby.typeHintVariables": {
          "type": "array",
          "default": [
            "$page",
            "$site",
            "$kirby"
          ],
          "description": "Variables to include in the PHPDoc type hint block",
          "items": {
            "type": "string"
          }
        },
        "kirby.enableBlueprintValidation": {
          "type": "boolean",
          "default": true,
          "description": "Enable JSON Schema validation for Kirby Blueprint files"
        },
        "kirby.blueprintSchemaPath": {
          "type": "string",
          "default": "",
          "description": "Path to custom Blueprint JSON Schema (leave empty to use bundled schema)"
        },
        "kirby.showSnippetCodeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens links above snippet() calls for quick navigation"
        },
        "kirby.enableTailwindIntegration": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic Tailwind CSS integration for PHP templates"
        },
        "kirby.showBlueprintFieldCodeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens with Blueprint fields in template files"
        },
        "kirby.showBlueprintFieldTypes": {
          "type": "boolean",
          "default": false,
          "description": "Show field types in Blueprint field CodeLens"
        },
        "kirby.blueprintFieldDisplayLimit": {
          "type": "number",
          "default": 5,
          "description": "Maximum number of Blueprint fields to display before truncating"
        },
        "kirby.showControllerNavigation": {
          "type": "boolean",
          "default": true,
          "description": "Show navigation to controller files from templates"
        },
        "kirby.showModelNavigation": {
          "type": "boolean",
          "default": true,
          "description": "Show navigation to model files from templates"
        },
        "kirby.enableBlueprintTemplateSync": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic prompts to create matching Blueprint/Template files"
        },
        "kirby.syncPromptBehavior": {
          "type": "string",
          "enum": [
            "ask",
            "never",
            "always"
          ],
          "default": "ask",
          "description": "How to handle Blueprint/Template synchronization: 'ask' (prompt user), 'never' (disable prompts), 'always' (auto-create files)"
        },
        "kirby.syncCreateController": {
          "type": "boolean",
          "default": false,
          "description": "Automatically create a controller file when creating a template from a Blueprint"
        },
        "kirby.syncCreateModel": {
          "type": "boolean",
          "default": false,
          "description": "Automatically create a model file when creating a template from a Blueprint"
        },
        "kirby.syncIgnoreFolders": {
          "type": "array",
          "default": [],
          "description": "Folder patterns to exclude from Blueprint/Template synchronization detection",
          "items": {
            "type": "string"
          }
        },
        "kirby.enableBuildIntegration": {
          "type": "boolean",
          "default": true,
          "description": "Enable build integration features for managing npm build scripts"
        },
        "kirby.buildCommand": {
          "type": "string",
          "default": "",
          "description": "Custom build command to run (leave empty to auto-detect from package.json scripts)"
        },
        "kirby.buildAutoStart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start the build watcher when opening a Kirby workspace"
        },
        "kirby.buildAutoStartScript": {
          "type": "string",
          "enum": [
            "dev",
            "watch",
            "build"
          ],
          "default": "dev",
          "description": "Which npm script to use for auto-start (dev, watch, or build)"
        },
        "kirby.buildAutoStartDelay": {
          "type": "number",
          "default": 2000,
          "description": "Delay in milliseconds before auto-starting build watcher (allows project to load)"
        },
        "kirby.enableBuildOutputParsing": {
          "type": "boolean",
          "default": true,
          "description": "Enable build output parsing for watch mode detection and metrics"
        },
        "kirby.buildToolPatterns": {
          "type": "object",
          "default": {},
          "description": "Custom build tool output patterns for detection (advanced users only)"
        },
        "kirby.monitorAllTerminals": {
          "type": "boolean",
          "default": false,
          "description": "Monitor all VS Code terminals for build processes (not just extension-created ones)"
        },
        "kirby.showBuildMetrics": {
          "type": "boolean",
          "default": true,
          "description": "Show build duration and rebuild count in status bar tooltip"
        },
        "kirby.syncBlockSnippets": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic prompts to create matching block Blueprint/Snippet files"
        },
        "kirby.syncFieldSnippets": {
          "type": "boolean",
          "default": false,
          "description": "Enable automatic prompts to create matching field Blueprint/Snippet files (opt-in)"
        },
        "kirby.syncBlockNestingStrategy": {
          "type": "string",
          "enum": [
            "auto",
            "flat",
            "nested"
          ],
          "default": "auto",
          "description": "Block snippet organization strategy: 'auto' (detect from existing files), 'flat' (dot notation), 'nested' (directories)"
        },
        "kirby.enableSnippetControllers": {
          "type": "boolean",
          "default": true,
          "description": "Enable support for Kirby Snippet Controller plugin (navigation, type-hints, and CodeLens for snippet controllers)"
        },
        "kirby.enableApiIntelliSense": {
          "type": "boolean",
          "default": true,
          "description": "Enable intelligent autocompletion and documentation for Kirby API classes via Intelephense"
        },
        "kirby.kirbyVersion": {
          "type": "string",
          "default": "4.0",
          "description": "Kirby CMS version for API stubs (currently only 4.0 is supported)"
        },
        "kirby.customStubsPath": {
          "type": "string",
          "default": "",
          "description": "Custom path to Kirby API stub files (leave empty to use bundled stubs)"
        }
      }
    },
    "yamlValidation": [
      {
        "fileMatch": "**/site/blueprints/**/*.yml",
        "url": "./out/schemas/blueprint.schema.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ && npm run copy-schemas && npm run copy-stubs",
    "copy-schemas": "mkdir -p out/schemas && cp src/schemas/*.json out/schemas/ && cp src/schemas/LICENSE out/schemas/",
    "copy-stubs": "mkdir -p out/stubs && cp -r src/stubs/kirby-api out/stubs/ && cp src/stubs/README.md out/stubs/",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.36.0",
    "husky": "^9.1.7",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "node-pty": "^1.0.0"
  }
}
